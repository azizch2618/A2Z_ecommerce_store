import { AccountAddressesPageView } from "@/components/account/account-addresses-page-view";

export const metadata = {
  title: "Saved addresses | A2Z Tools",
  description: "Manage your delivery and billing addresses.",
};

export default function AccountAddressesPage() {
  return <AccountAddressesPageView />;
}
