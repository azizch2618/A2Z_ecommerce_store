import { WishlistPageView } from "@/components/wishlist";

export const metadata = {
  title: "Wishlist | A2Z Tools",
  description:
    "Your saved products with trade pricing, stock status, and quick add-to-cart actions.",
};

export default function WishlistPage() {
  return <WishlistPageView />;
}
