import { CheckoutPageView } from "@/components/checkout";

export const metadata = {
  title: "Checkout | A2Z Tools",
  description:
    "Secure Australian checkout with GST-inclusive pricing, state-based delivery, and multiple payment options.",
};

export default function CheckoutPage() {
  return <CheckoutPageView />;
}
