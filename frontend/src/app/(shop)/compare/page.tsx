import { ComparePageView } from "@/components/compare";

export const metadata = {
  title: "Compare products | A2Z Tools",
  description:
    "Compare up to four networking products side by side — specifications, trade pricing, and stock status.",
};

export default function ComparePage() {
  return <ComparePageView />;
}
