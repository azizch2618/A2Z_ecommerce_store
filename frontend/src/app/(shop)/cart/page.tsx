import { CartPageView } from "@/components/cart";

export const metadata = {
  title: "Shopping Cart | A2Z Tools",
  description:
    "Review your cart — GST-inclusive pricing, shipping estimates, and secure Australian checkout.",
};

export default function CartPage() {
  return <CartPageView />;
}
